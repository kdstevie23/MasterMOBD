import UIKit

var greeting = "Hello, playground"

func calcNum(powerOf: Int) -> [Int] {
    
    
    var powerArray : [Int] = []
        
    for num in 0...powerOf {
        powerArray.append(Int(pow(double)))
        }
    }
    
}
